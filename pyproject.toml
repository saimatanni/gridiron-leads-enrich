[project]
name = "gridiron-enrich"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "ddgs>=9.14.2",
    "dnspython>=2.8.0",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "selectolax>=0.4.8",
    "streamlit>=1.57.0",
    "tenacity>=9.1.4",
]
# playwright + weasyprint omitted from the local copy:
#   playwright pulls a 150MB chromium download; we use system Chrome instead
#   weasyprint needs system libs we can't easily install
